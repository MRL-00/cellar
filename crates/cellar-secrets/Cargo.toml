[package]
name = "cellar-secrets"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Cellar — credential storage (OS keychain, encrypted fallback)"
publish = false
