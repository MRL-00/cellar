[package]
name = "cellar-sql"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Cellar — SQL parsing, formatting, dialect awareness"
publish = false

[dependencies]
sqlparser = "0.52"
