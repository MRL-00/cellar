[package]
name = "cellar-gpui-grid-prototype"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
gpui = { version = "0.2.2", default-features = false, features = ["font-kit", "wayland", "x11"] }

[workspace]
